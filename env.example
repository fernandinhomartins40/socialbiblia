# Configurações de Deploy - Biblicai
# Copie este arquivo para .env para configurar o deploy

# Configurações da VPS
VPS_HOST=your-vps-ip-or-domain
VPS_USER=root
VPS_PORT=22
VPS_PATH=/opt/biblicai
SSH_KEY=$HOME/.ssh/id_rsa

# Configurações Docker
DOCKER_REGISTRY=
DOCKER_IMAGE_TAG=latest

# Configurações de Produção
NODE_ENV=production
PORT=3000

# Configurações do Plugbase Backend (se rodando via Docker)
PLUGBASE_IMAGE=plugbase:latest
PLUGBASE_PORT=3001

# Configurações de Banco de Dados (se necessário para o Plugbase)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# REDIS_URL=redis://localhost:6379

# Configurações de Segurança
# JWT_SECRET=your-super-secret-jwt-key
# SESSION_SECRET=your-session-secret