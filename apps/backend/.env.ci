# ==============================================
# CONFIGURAÇÕES DE CI/CD - BACKEND PLUGBASE
# ==============================================

# Database Configuration (Mock/Test Database)
DATABASE_URL=postgresql://ci_user:ci_password@localhost:5432/ci_test_db?schema=public
POSTGRES_DB=ci_test_db
POSTGRES_USER=ci_user
POSTGRES_PASSWORD=ci_password
POSTGRES_PORT=5432

# API Configuration
APP_URL_HOST=0.0.0.0
APP_URL_PORT=3344
SSL_ALLOW=false
API_PREFIX=api
API_JSON_LIMIT=5mb
API_EXT_URLENCODED=false
CORS_ALLOW_ORIGIN=*

# JWT Configuration (CI/Test secrets)
JWT_SECRET_USER=CI_Test_JWT_Secret_User_2024
JWT_SECRET_DEVICE=CI_Test_JWT_Secret_Device_2024
JWT_EXPIRED_IN=24h

# Security Configuration
BCRYPT_SALTROUNDS=4
RATE_LIMIT_MAX=1000
RATE_LIMIT_WINDOW=15

# Debug Configuration (ENABLED FOR CI)
DEBUG_HTTP_REQUEST=true
DEBUG_HTTP_CONNECTION=false

# Email Configuration (DISABLED FOR CI)
EMAIL_USER=test@example.com
EMAIL_PASSWORD=
EMAIL_SERVICE=gmail
EMAIL_OAUTH_CLIENT_ID=
EMAIL_OAUTH_CLIENT_SECRET=
EMAIL_OAUTH_REFRESH_TOKEN=

# Environment
NODE_ENV=test
TZ=America/Sao_Paulo
PORT=3344 